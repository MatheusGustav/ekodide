<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Rede local + descoberta UDP (porta 8779). -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />
    <!-- CHANGE_WIFI_MULTICAST_STATE: necessária pro broadcast UDP E é o pré-requisito
         do foreground service tipo connectedDevice (mata dois coelhos). -->
    <uses-permission android:name="android.permission.CHANGE_WIFI_MULTICAST_STATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- Servidor passivo de longa duração (entra na Etapa "segundo plano").
         Tipo connectedDevice: SEM teto de 6h e PODE subir no boot (ao contrário de dataSync). -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <!-- Isenção de bateria (passa a rede pelo Doze). Restrita na Play, mas OK por ser
         APK sideload — instalação direta não passa pela revisão da loja. -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Ekodide">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Servidor passivo de longa duração. connectedDevice: sem teto de 6h, pode
             subir no boot. exported=false: ninguém de fora aciona o serviço. -->
        <service
            android:name=".ServidorService"
            android:exported="false"
            android:foregroundServiceType="connectedDevice" />

        <!-- Sobe o serviço no boot. exported=true: o broadcast vem do sistema.
             Nos OEMs (MIUI) só chega com "autostart" liberado. -->
        <receiver
            android:name=".BootReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

    </application>
</manifest>
